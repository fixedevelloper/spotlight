name: CI/CD Spring Boot + Docker + VPS

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Récupérer le code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Installer Java 17
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      # 3️⃣ Build Maven
      - name: Build Maven project
        run: mvn clean package -DskipTests

      # 4️⃣ Build Docker image avec tag correct
      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/softlight:latest .

      # 5️⃣ Login DockerHub
      - name: DockerHub Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 6️⃣ Push Docker image
      - name: Push Docker image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/softlight:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      # 7️⃣ Déployer sur VPS via SSH
      - name: Deploy on VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ~/softlight
            docker-compose pull
            docker-compose down
            docker-compose up -d
            docker image prune -f
